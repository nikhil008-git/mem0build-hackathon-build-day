# Frontend

Next.js app with Better Auth. Runs on http://localhost:3000.

Requires `apps/frontend/.env` — see the root [README](../../README.md).

```sh
npm run dev
```

From the repo root:

```sh
npx turbo dev --filter=frontend
```
